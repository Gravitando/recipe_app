name: recipe_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  sqflite: ^2.3.0
  path: ^1.8.3
  shared_preferences: ^2.2.2
  http: ^1.1.0
  google_fonts: ^6.1.0
  intl: ^0.18.1
  image_picker: ^1.0.7
  table_calendar: ^3.0.9        # For meal planner calendar
  flutter_rating_bar: ^4.0.1    # For recipe ratings

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true